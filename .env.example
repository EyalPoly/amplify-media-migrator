# AWS Configuration
AWS_REGION=us-east-1
AMPLIFY_API_ENDPOINT=https://xxx.appsync-api.region.amazonaws.com/graphql

# Google Drive Configuration
GOOGLE_APPLICATION_CREDENTIALS=~/.amplify-media-migrator/google_credentials.json

# Logging
LOG_LEVEL=INFO

# Optional: Override default config file location
# CONFIG_PATH=~/.amplify-media-migrator/config.json